name: rice_classification
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.11.0  # <--- Version Update
  image_picker: ^1.1.2
  image: ^4.2.0       # Image resize o process korar jonno

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Assets configure kora holo
  assets:
    - assets/rice_model_new.tflite
    - assets/labels.txt